package model.Characters;

public class Digger extends Character {

    /**
     * Constructor: Creates a new card with "name" Digger and "holder" holder
     * <p>
     * Postcondition: Card is created
     *
     * @param id Player's id
     */
    public Digger(int id, String color) {

        super(id, "Digger", color);
    }
}
