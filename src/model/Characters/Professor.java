package model.Characters;

public class Professor extends Character {

    /**
     * Constructor
     * <p>
     * Postcondition: Creates a new card with "name" Professor and "holder" holder
     *
     * @param id Player's id
     */
    public Professor(int id, String color) {

        super(id, "Professor", color);
    }
}
